# FAS

EntityFrameworkCore\Add-Migration AddedPrincipalTables -Context BBEIContext -OutputDir Migrations/

PowerShell:
Get-ChildItem -Path C:\home\site\wwwroot\* -Recurse | Where-Object { $_.Name -notlike "fas_data.*" } | Remove-Item -Recurse -Force

CMD:
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py

Twilio -> must add Caller IDs to send wa notifies

SendGrid -> fas-notifier@email.com


TODO:
ADMIN reset standing
Calcolo voti (to test)

DA CAPIRE:
notifications ( > Program.SRABidTS && < no formazione / giornata calcolata)
